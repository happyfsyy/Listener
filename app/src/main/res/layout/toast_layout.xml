<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingTop="30dp" android:paddingBottom="30dp"
    android:paddingStart="100dp" android:paddingEnd="100dp"
    android:background="@color/colorPrimary">
    <ImageView
        android:id="@+id/toast_img"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center_horizontal"
        android:src="@drawable/tag_selected"
        android:scaleType="centerCrop"/>
    <TextView
        android:id="@+id/toast_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/commonTextSize"
        android:textColor="@color/textColor"
        android:layout_gravity="center_horizontal"
        android:text="@string/toast_network_exception"
        android:layout_marginTop="@dimen/inner_padding"/>
</LinearLayout>